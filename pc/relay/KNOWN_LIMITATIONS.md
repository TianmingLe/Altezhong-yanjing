# Known Limitations (MVP)

- No auth on WebSocket endpoint
- Single-client assumption
- In-memory sessions only (server restart loses state)
- Feature-only (frame_type=0x02); jpeg_chunk reserved but not implemented

